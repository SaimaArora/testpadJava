class Solution {
    public int minMoves2(int[] nums) {
        int operations = 0;
        Arrays.sort(nums);
        int n = nums.length;
        int med = nums[n/2];
        for(int i = 0; i < n; i++) {
            operations+=Math.abs(med - nums[i]);
        }
        return operations;
    }
}
